defmodule Beat do
  @moduledoc """
  Beat is an empty application intended to be filled, through config, with a
  few critical GenServers that you need to run separately from your app. This
  would typically be for health checks and a heart module.

  This library doesn't offer a generic implementation because you should
  consider the needs of your application.
  """
end
